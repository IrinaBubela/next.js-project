export default function PrivateRoute() {
    return <h1>You cannot view this in the browser</h1>
}

//_lib - makes component invisible for routing
//it creates private folder

// => we can separate logic from UI
// => organizing internal files
// => avoiding potential naming conflicts

//you want to have __ in your URL? -put %5F